[package]
name = "token-escrow"
version = "0.1.0"
edition = "2024"

[dependencies]
litesvm = "0.11.0"
solana-program = "3.0.0"
solana-sdk = "3.0.0"

[dev-dependencies]
tokio = { version = "1.50.0", features = ["rt-multi-thread", "macros"] }
